# ---------------------------------------------------------------- #
#                  1. CMake/Build Artifacts                        #
# ---------------------------------------------------------------- #

# 1.1 Ignore the default out-of-source build directory
# This directory contains the Makefile, CMakeCache.txt, etc.
# We explicitly set the convention to 'build'
/build/

# 1.2 Ignore the contents of any subdirectory named 'bin'
# This is where your compiled executables go, as set by CMAKE_RUNTIME_OUTPUT_DIRECTORY
# It ignores './bin' in the project root AND 'bin' in the build directory.
**/bin/

# 1.3 Ignore CMake's generated files if an in-source build is accidentally performed
# These files are always generated by the 'cmake ..' configuration step.
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
CTestTestfile.cmake
Makefile
install_manifest.txt
compile_commands.json

# 1.4 Ignore common object/binary files that may be created outside the build folder
# CUDA/C++ object files
*.o
*.obj
# Precompiled header files
*.gch
*.pch
# Libraries (if not in a dedicated and tracked output folder)
*.so
*.a
*.lib
*.dll
*.dylib

# ---------------------------------------------------------------- #
#                     2. IDE/Editor Files                          #
# ---------------------------------------------------------------- #

# Visual Studio (often used for CUDA development on Windows)
.vs/
*.VC.db
*.pdb
*.suo
*.user

# CLion (common cross-platform C++ IDE, uses .idea/ for project metadata)
.idea/

# VS Code
.vscode/

# Vim/NeoVim swap and backup files
*~
*.swp
*.bak


# 
pytorch_env